#include<bits/stdc++.h>
using namespace std;
int main()
{
    string first, second;
    int f=0,s=0;
    cin>>first>>second;
    if(first.length() >= second.length())
        cout<<"go";
    else
        cout<<"no";
    return 0;
}
